default:
  description: 'Clean folder build'
  tasks:
    - clean
develop:
  description: 'Copy/creation css/js default files'
  tasks:
    - 'concurrent:buildDev'
imagemin:
  description: 'Copy image from img/ and compress'
  tasks:
    - 'concurrent:imgMin'
dec:
  description: 'test'
  tasks:
    - 'concurrent:mathJax'
production:
  description: 'Production build'
  tasks:
    - 'concurrent:buildDev'
    - 'concurrent:buildProd'
    - 'concurrent:imgMin'
    - 'concurrent:mathJax'
